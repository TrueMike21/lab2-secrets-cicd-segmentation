[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lab2-segmentation"
version = "0.1.0"
description = "Lab 2: Secrets Injection, CI/CD, and Segmentation Model Training"
requires-python = ">=3.11"